services:
  ros2-dev:
    image: ros2-isaac:latest
    container_name: ros2-isaac-dev
    tty: true
    network_mode: "host"
    ports: 
      - "9090:9090"
    environment:
      - ROS_DOMAIN_ID=0
    volumes:
      - $HOME/dev/IsaacSim-ros_workspaces/jazzy_ws:/jazzy_ws  
